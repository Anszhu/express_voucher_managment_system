import fs from 'node:fs'; import app from './app.js'; import { config } from './lib/config.js'; import { logger } from './lib/logger.js'; import { prisma } from './lib/prisma.js';
fs.mkdirSync('uploads',{recursive:true}); const server=app.listen(config.port,()=>logger.info({port:config.port,environment:config.env},'API listening')); const shutdown=async(signal:string)=>{logger.info({signal},'Shutdown started');server.close();await prisma.$disconnect();process.exit(0);};process.on('SIGINT',()=>void shutdown('SIGINT'));process.on('SIGTERM',()=>void shutdown('SIGTERM'));
